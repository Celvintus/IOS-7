Capture List позволяет захватывать значения из вне и использовать их внутри замыкания или функции. 

var outsideValue = 10

let closure = { [outsideValue] in
    // Используем захваченное значение из внешнего контекста
    print("Захваченное значение: \(outsideValue)")
}

// Изменяем значение внешней переменной
outsideValue = 20

// Вызываем замыкание
closure()

В примере переменная outsideValue должна быть захвачена и использована внутри замыкания. 
Значение переменной, захваченной в момент создания замыкания, сохраняется и не изменяется после изменения переменной outsideValue.
